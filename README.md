# International Student Job Board

Roles at startups and scaleups founded in Victoria, Australia : : Mapped with potential visa pathways and skills assessments it can lead to!

---

### Run

```bash
cd job-board && npm install && npm start          # dev at localhost:3000
npm run build:pages                               # build into ../docs for Pages
```

---

### Deploy

Commit the updated `docs/` folder and push to deploy.

Made with ❤️ by Milindi Kodikara
